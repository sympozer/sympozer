/**
 * Context module configuration
 * route <--> template <--> controller
 *
 * @type {config}
 */
angular.module('contextualizationApp')
  .config(
  ['$routeProvider',
    function ($routeProvider)
    {
      //@TODO Florian : delete file ? Pas de routage pour le controller de context, non ?
    }
  ]);